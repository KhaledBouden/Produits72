<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>

    <body>
    
        <style>
        .custom-line {
        border-left: 1px solid #555;
        height: 90%;
    }
    .overflow-auto {
    overflow: auto;
    word-wrap: break-word;
}
    </style>


    <header>
    <div class="px-3 py-2 text-bg-dark border-bottom">
      <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
          <a href="{{path('app_catalogue')}}" class="d-flex align-items-center my-2 my-lg-0 me-lg-auto text-white text-decoration-none">
            <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
            <h2>72</h2>
            <span class="fs-4 px-2">Produits72</span>
          </a>

          <ul class="nav col-12 col-lg-auto my-2 justify-content-center my-md-0 text-small">
           
                    <li>
            <a href="{{ path('app_catalogue') }}" class="nav-link {% if app.request.attributes.get('_route') == 'app_catalogue' %}text-blue{% else %}text-white{% endif %}  d-flex flex-column align-items-center">
             <i class="bi bi-house d-block mx-auto mb-1 display-6"></i>
               Acceuil
                </a>
            </li>
            
           <li>
            <a href="{{path('show_cart')}}" class="nav-link active d-flex flex-column align-items-center custom-line pe-2 me-2 text-white">
                <i class="bi bi-cart d-block mx-auto mb-1 display-6"></i>
                Panier
            </a>
            </li>
            

            <li>
            <a href="{{path('app_produit_index')}}" class="nav-link {% if app.request.attributes.get('_route') == 'app_produit_index' %}text-blue{% else %}text-white{% endif %}   custom-line d-flex flex-column align-items-center">
                <i class="bi bi-person-circle d-block mx-auto mb-1 display-6"></i>
                {% if app.user %} {{app.user.fullname}} {% else %} Profile {% endif %}
            </a>
            </li>
            
          </ul>
        </div>
      </div>
    </div>
    <div class="px-3 py-2 border-bottom mb-3">
    <div class="container d-flex flex-wrap justify-content-between align-items-center">
        <div class="d-flex">
        {% if app.request.attributes.get('_route') == 'app_catalogue' %}
            <form class="form-inline mb-4">
                <div class="form-group mr-2">
                    <label for="searchTerm">Rechercher</label>
                    <input type="text" class="form-control" id="searchTerm" name="t" value="{{ searchTerm }}">
                </div>
                <!-- Add the search button here if needed -->
            </form>
            {% endif %}
        </div>
        <div class="text-end">
            <a href="{{ path('app_order_index') }}" type="button" class="btn btn-light text-dark me-2">Les commandes</a>
            <a href="/{{ app.user ? 'logout' : 'login' }}" type="button" class="btn btn-light text-dark me-2">{{ app.user ? 'Deconnecter' : 'Se Connecter' }}</a>
            {% if not app.user %}
                <a href="{{ path('secuirity.registration') }}" type="button" class="btn btn-primary">S'inscrire</a>
            {% endif %}
        </div>
    </div>
</div>

  </header>
        <main>
        {% block body %}
         
        {% endblock %}
        </main>
        
        <div class="container">
  <footer class="py-3 my-4">

    <ul class="nav justify-content-center border-bottom pb-3 mb-3">
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">Home</a></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">Features</a></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">Pricing</a></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">FAQs</a></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">About</a></li>
    </ul>
    <p class="text-center text-body-secondary">© 2023 Company, Inc</p>
  </footer>
</div>

        <script src="{{ asset('assets/js/cart_notification.js') }}"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    </body>
</html>
