{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
    <h1>Modifier {{produit.titre}}</h1>
 {{ include('produit/_form.html.twig', {'button_label': 'Modifier'}) }}
   

   

   
</form>
<div class="p-2">

 <form method="post" action="{{ path('app_produit_delete', {'id': produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.id) }}">
    <button class="btn btn-danger"><i class="bi bi-trash"></i>supprimer</button>
    
<a href="{{ path('app_produit_index') }}" class="btn btn-primary"><i class="bi bi-arrow-left"></i>Retour</a>
</div>
{% endblock %}
